<Window x:Class="Pasti.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pasti Reader / Writer 0.5" Height="600" Width="900">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel  Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch" Height="20" VerticalAlignment="Top">
            <Button Margin="0,0,10,0" Content="Pasti File" Width="100" Click="btFileClick" />
            <Button Margin="0,0,10,0" Content="Write File" Width="100" Click="btWriteClick" />
            <TextBox Name="fileName" Margin="5,0" BorderThickness="0" />
        </StackPanel>
        <TextBox Grid.Row="1" Name="infoBox" HorizontalAlignment="Stretch" TextWrapping="Wrap" Text="" VerticalAlignment="Stretch"  VerticalScrollBarVisibility="Auto" Grid.RowSpan="2"/>
    </Grid>
</Window>
